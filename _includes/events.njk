<section>
  <h2>Spring 2026</h2>
  {#
  <div class="event-container">
    <ul id="events">
      {% for event in events %}

      <li>
        {{ event.date | toLocalDate }} @ {% if event.url %}
        <a href="{{ event.url }}"> {{ event.host }} </a>
        {% else %} {{ event.host | formatHost }} {% endif %}
      </li>

      {% endfor %}
    </ul>
  </div>
  #}
  <div class="event-announcement">
    <p>
      After 10 years of meetups, we're taking a break for spring 2026. We will
      be back in the fall unless someone fills the void in the meanwhile.
    </p>
    <p>
      We want to thank everyone who's been part of our journey for the past
      decade: none of this would have happened without you.
    </p>
  </div>
  <div style="text-align: center">
    {#
    <a href="{{ '/history' | url }}">full history</a>
    #}
    <a href="{{ '/history' | url }}">see all our events so far</a>
  </div>
</section>
